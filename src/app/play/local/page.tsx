export default function LocalPage(): React.ReactElement {
  return (
    <main className="flex min-h-dvh items-center justify-center">
      <p className="text-muted-foreground">Multiplayer locale — in arrivo</p>
    </main>
  )
}
